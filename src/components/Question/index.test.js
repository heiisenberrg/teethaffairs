import 'react-native';

test('primary user can able to raise the questions on behalf of the family members', () => {
	expect(1).toEqual(1);
});

test('will redirect to the payment page while submitting the questions', () => {
	expect(1).toEqual(1);
});

test('questions will be published to the doctors', () => {
	expect(1).toEqual(1);
});
