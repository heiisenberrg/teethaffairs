import 'react-native';
// import React from 'react';
// import SignUpForm from '../SignUpForm';

// import renderer from 'react-test-renderer';

// test('renders correctly', () => {
// 	const tree = renderer.create(<SignUpForm />).toJSON();
// 	expect(tree).toMatchSnapshot();
// });

test('contains registration form', () => {
	expect(1).toEqual(1);
});

test('submit button remains disabled until mantadary fields are filled', () => {
	expect(1).toEqual(1);
});

test('renders the error message if that field does not contain expected values', () => {
	expect(1).toEqual(1);
});

test('the user who sign up will be considered as the primary member of the family', () => {
	expect(1).toEqual(1);
});

test('will redirect to login page once the received values get verified', () => {
	expect(1).toEqual(1);
});

test('clears form once registration is done', () => {
	expect(1).toEqual(1);
});
