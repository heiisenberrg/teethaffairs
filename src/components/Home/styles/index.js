import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	container: { height: 1200, backgroundColor: '#eee', padding: 10 },
	buttonWrap: { marginTop: 10 }
});

export default styles;
