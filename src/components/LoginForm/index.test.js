import 'react-native';

test('contains login form', () => {
	expect(1).toEqual(1);
});

test('username and password fields are mandatory', () => {
	expect(1).toEqual(1);
});

test('renders the error message if that field does not contain expected values', () => {
	expect(1).toEqual(1);
});

test('submit button remains disabled until mantadary fields are filled', () => {
	expect(1).toEqual(1);
});

test('will navigate to page if the received values contain the verified values', () => {
	expect(1).toEqual(1);
});

test('clears form once logged in', () => {
	expect(1).toEqual(1);
});
