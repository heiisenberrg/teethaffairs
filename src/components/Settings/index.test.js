import 'react-native';

test('patients can manage the profile', () => {
	expect(1).toEqual(1);
});

test('patients can manage the history', () => {
	expect(1).toEqual(1);
});

test('patients can manage the payment details', () => {
	expect(1).toEqual(1);
});
