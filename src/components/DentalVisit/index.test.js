import 'react-native';

test('patients can able to add reason for the visit', () => {
	expect(1).toEqual(1);
});
test('patients can able to add detailed decription', () => {
	expect(1).toEqual(1);
});
test('patients can able to add experience of the visit', () => {
	expect(1).toEqual(1);
});

test('patients can able to add dentist name', () => {
	expect(1).toEqual(1);
});

test('patients can able to add fees paid', () => {
	expect(1).toEqual(1);
});

test('patients can able to add details of insurance they used', () => {
	expect(1).toEqual(1);
});

test('all the details will be saved', () => {
	expect(1).toEqual(1);
});

test('all the details can be seen in history page', () => {
	expect(1).toEqual(1);
});
