import 'react-native';

test('contains dental note form', () => {
	expect(1).toEqual(1);
});

test('form fields are related to symptoms / issues', () => {
	expect(1).toEqual(1);
});

test('patients need to provide details for consultation', () => {
	expect(1).toEqual(1);
});

test('upload images through camera or from the gallery ', () => {
	expect(1).toEqual(1);
});

test('can use this notes to initiate a remote consultation ', () => {
	expect(1).toEqual(1);
});

test('can save this notes for the future reference ', () => {
	expect(1).toEqual(1);
});
