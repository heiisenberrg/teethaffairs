import 'react-native';

test('primary user can add his/her family memebers', () => {
	expect(1).toEqual(1);
});

test('primary user needs to provide some details for onboarding a new family member', () => {
	expect(1).toEqual(1);
});

test('username and PIN will be allocated for the newly added family members', () => {
	expect(1).toEqual(1);
});

test('family members can use the username and pin assigned to them to login the app', () => {
	expect(1).toEqual(1);
});
