import 'react-native';

test('contains question, note and visit module', () => {
	expect(1).toEqual(1);
});
test('renders recent visit/history of the logged in user', () => {
	expect(1).toEqual(1);
});

test('each module contains the relavent information', () => {
	expect(1).toEqual(1);
});

test('primary user will have an option to select any one of the family members', () => {
	expect(1).toEqual(1);
});

test('primary user can check the family members dental visit', () => {
	expect(1).toEqual(1);
});
test('primary user can check the family members history of Q&A', () => {
	expect(1).toEqual(1);
});
test('primary user can check the family members notes', () => {
	expect(1).toEqual(1);
});

test('primary user can check the family members reminders', () => {
	expect(1).toEqual(1);
});

test('family members can only able to see their respesctive details, not all members details', () => {
	expect(1).toEqual(1);
});
