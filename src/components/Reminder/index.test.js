import 'react-native';

test('patients can set the reminders for reminding about brush/floss ', () => {
	expect(1).toEqual(1);
});

test('patients can add the date and time for the reminders', () => {
	expect(1).toEqual(1);
});

test('patients can able to view all the reminders list', () => {
	expect(1).toEqual(1);
});

test('patients can manage the reminders whenever they want', () => {
	expect(1).toEqual(1);
});

test('on the time of event, patient will get alert', () => {
	expect(1).toEqual(1);
});

test('alert consists some specific messages', () => {
	expect(1).toEqual(1);
});

test('patients has to respond to one of the specific messages to stop the alert', () => {
	expect(1).toEqual(1);
});

test('primary user can able to set reminders on behalf of the family members', () => {
	expect(1).toEqual(1);
});
